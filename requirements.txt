# ─────────────────────────────────────────────────────────────
# WingID — Python Dependencies
# ─────────────────────────────────────────────────────────────
# Install order matters:
#   1. pip install -r requirements.txt
#   2. pip install --pre torch torchvision torchaudio \
#          --index-url https://download.pytorch.org/whl/nightly/cu128
#
# PyTorch is NOT listed here because the correct wheel depends
# on your CUDA version. See README.md → Installation for details.
# ─────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── Computer Vision ───────────────────────────────────────────
opencv-python>=4.9.0

# ── Machine Learning & Inference ──────────────────────────────
ultralytics>=8.2.0          # YOLOv11 — downloads weights on first run
transformers>=4.40.0        # HuggingFace CLIP zero-shot pipeline
accelerate>=0.29.0          # Required by transformers pipeline on GPU

# ── Image Processing ──────────────────────────────────────────
Pillow>=10.3.0

# ── Utilities ─────────────────────────────────────────────────
numpy>=1.26.0
requests>=2.31.0            # Used internally by ultralytics for weight download
tqdm>=4.66.0                # Progress bars (ultralytics dependency)
pyyaml>=6.0.1               # ultralytics config parsing
psutil>=5.9.0               # System resource monitoring

# ── PDF Export (optional — for future Intel Dossier feature) ──
# reportlab>=4.1.0

# ── TensorRT (Windows only, install separately via pip) ────────
# tensorrt is bundled with CUDA toolkit on Windows.
# If you need to install it explicitly:
# pip install tensorrt --index-url https://pypi.nvidia.com
